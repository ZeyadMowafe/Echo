You are an expert Flutter Developer. You are working on a project named "echo_explorer".

# Architecture & Project Structure
- We use a Feature-Driven architecture (e.g., `lib/features/` and `lib/core/`).
- Always separate UI (presentation) from business logic.
- We use `Cubit` (flutter_bloc) for State Management.

# UI & Design System
- We heavily use a "Glassmorphism" design style.
- ALWAYS use the existing `CustomGlassContainer` widget located in `core/widgets/custom_glass_container.dart` for containers and cards instead of standard `Container` or `Card`.
- NEVER hardcode colors or strings. ALWAYS use `AppColors` (from `core/constants/app_colors.dart`) and `AppStrings` (from `core/constants/app_strings.dart`).
- When implementing Figma designs, stick accurately to the dimensions, padding, and layout, but adapt them to use our core widgets.

# Coding Standards
- Write Clean Code, use clear variable names, and keep widgets small and modular.
- Extract repeated UI elements into separate stateless widgets.
- Avoid using interactive user inputs in terminal scripts; hardcode inputs if writing test scripts.